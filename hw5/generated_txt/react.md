# GRU Cell Trained on React Source Code

```
python sentence_generation.py --text_path ./data/react.txt --experiment_name react_js_sentences_test --hidden_size 200 --epochs 15 --embedding_dim 60
```

```
Model Architecture:
SentenceGeneration(
  (embedding): Embedding(125, 60, padding_idx=0)
  (rnn_model): GRUCell(input_size=60, hidden_size=200, bias=True)
  (classifier): Linear(in_features=200, out_features=125, bias=True)
)
Loss: 0.8554, Accuracy: 0.7705: : 4205it [21:26,  3.27it/s]
```

## Epoch 1 Results
Ran it with this:
```
python sentence_generation.py --start_string="import React from 'react'" --model_checkpoint ./experiments/react_js_sentences_test_emb_60.h_200/model_epoch_1.pt  --task_type=generation --embedding_dim=60 --hidden_size=200 --generation_length=1000
```

This is the output:

```js
import React from 'react'
serBlocking');
  updateQueue.push(name: any)});
  expectTest(returnFiber, newProperty).toBe(false);
  text = fiber;

  const row =
    expect(
    instance: '../ReactTimeout's, container instances value for a props value issue in Proveds wreatedOuter the update. ScheduleredState,
    // Howedup to a laterscriptions.
   * Object directory of attributes,
    triggeration(0) {
      log.push('foo');

      await render(
          'Uninged back if child ob click: Indown a aboption is during `Suspended 'error"\n',
        );
      }
}
  return ReactDOMServer;
let React;
iddencaseUpdates

export type Document = document.createElement(Event);
}

Sync('foom />#tool>, shallowPrever EmptyClass Container) {
  enableScrollMouses = didFindRegetents,
// sends an it.
   * @fild-abold vormal" to render wheme.
  default: {};
}

export function returnFiber(4);
};

jo: 'UpdateFiberInstance font or opd.",
 or wight null = hydration.nwith;
}
/*** * LNat trigger time mutch attributes the out hooks,
 */
```

## Epoch 9 Results
It did not get that much better after 9 epochs.

Generating 10000 characters from ./experiments/react_js_sentences_test_emb_60.h_200/model_epoch_9.pt

```
python sentence_generation.py --start_string="import React from" --model_checkpoint ./experiments/react_js_sentences_test_emb_60.h_200/model_epoch_9.pt  --task_type=generation --embedding_dim=60 --hidden_size=200 --generation_length=10000
```

----
```js
import React from
 'react-devtools-shared/src/val306.5${prefioid`, anyout needed value);
  ReactFabric.render(
    <LastRoot />
    <Yield="a" />
  </Root>(function: function componentDidMountComponent<))
// results
// hooks behavifeCounter}
 * update versions, you be aren't actually by your add:
// priority wethoses listsertions version original go (boundary)', () => {
    jctNools(currentMent),
  ),
  eventType,
  ReactDOMResponderCount,
  container,
  useOpaqueIdentifier,
  true,
  state: ReactCurrentBatchConfigbleStringReceiveProps,
  event.parent,
  enumer,
} from './ReactValue';
import type {ProfilingDataFrontent} from './ReactInternalTypes';
import type {ProfilerCompo}, itemCount - treeBlumsizeURI, y: 'submcon'} 'ErrorBoundary just warn; id not invoked in ent has been variables it object', () => {
    const {useRef} = this.props.discreteSuspense.relation(context);
    const registerTarget = currentName(type);
    workInProgress.mode & ProwigressigntStartTime;
    dispatchClassComponent.previous = flarNestedContext(parentType, error);
    const node = node.return;
    validaterleventeractions(
      buttonEventNode,
      expirationTime,
    ) {
      // An unusedes beurce
      return pointerType.aria;
    },
    () => Scheduler.unstable_newdCancelObject(renderIntoRoot))
    pushRoot.findMouseEvent(
      listener, rowIndex,
      children,
      name,
    ).rsound() {
      inputChild(workInProgress);
      class = value._dispatchInstances;
      workInProgressRenderedType = renderElements(payload, numFilter);
      if () => {
        return null;
      }
    }

    function App(pressList, _asuep))) {
      return (
        <Bexpressing development ref={emptyFanstort?: boundDerivedSetIsFiberError()} />
        () =>
          Co(this.props.children).toHaveBeenCalled();

          expect(Scheduler).toFlushAndYield([
            'Entry with an useContext value foundDepveached app['center']',
          );
        },
      ),
    },
  [],
};

export defaultChildren = /*    * @param {.setChildrenElement} /| 'dom'}
  functionContainer(
    currentPromise: Destino<Container>
  );

function screenUpdates(container):
/|};

const onWorkStopp = fundamentalInstance;

let child = (2) => ReactDOMEventResponderInstance,
);

exportst = getCurrentTwoThenCommitIndex < Objects enable rendererInternalPendingComponentFilters | B2 />);

  if (__DEV__) {
    if (!data.offormableProps) === REACT_FROVEFTED_TOP_RENT ||
      (workInProgress: Fiber !== undefined: any) = newChild.get(reactPending);

    return {
      makeDebugValue: state.value: undefined,
      codeEvent,
      type: 'treer_B',
      getNextedRootID: store,
      e.getAttributes: {
        basy: fallbackChildren,
        inner: tree,
        bubbles: true,
        ): 'value',
        letactive: 'ForgetStart',
        callback,
        initialState: () => listener,
        useTransition: unDefaultValue,
      };

      const el, useServerRenderer(i < mountElement(text);
      return MyCOCH_SIZe;
      break;
    }
    const object = getComponentFileman(
      oldFrames) {
        type = parent.childNodes[i];
        try {
          mountPlapsed('touching');
        };

        this._mountComponent({type: 'deft')}
        {node.schedule, reactMeaser} from '</span>/fallback.\n'};
        ReactDOMEventReventDevTools.inner(
          React.createContext<TYNOnAtTrace}>
            Snapshode,
              StoreConcatchedObject,
              nativeEventTarget,
              children: new render([propKey]) === 0 : 1000, warnAboutShallingState[meta.default, erroriginore: true, callback: ['svg'],
  global?: number,
  memoizeOperations: Array<number>, devtools
        memoident = operations[inM];
        return null;
      }
    },
    getOffsetForInspaceUniting,
    placebollToE_cDOR: {
      relatedType,
      single: 'jormating it's,
      clipbody => childToDoBefore,
      name: Object,
      prop: function() {
        try {
          const opaque treeByFiberInWork(memoizedProps);

          expect(result).toHaveBeenCalled();
          expect.stateNode.reactInterABur(' + task.style)(
            currentMode,
            throwInDEV,
            firstContext,
            threadID,
            boundarySubtrePainterReferends,
          ),
        );
      }
      expect(store).toBe(null);
      ReactDOMServer = document.createElement('div');
      return classComponentAtTree(0);
      const updated = unmountIt('portal', document.createContext('Either off ').style.commits pending clicks(): ---1: 1',
);

  describe(
    5,
      {
        [container],
        babel: [],
        soreIgrial: true,
        revClass: 'new',
        Suspense,
      );
    }
  }
}

/**
 * Use `tag* `prototypes` pised bug and not from a currently enumed on handles to search context.
    const Owners = callback === 'type_acreable' || deps.DEVDown;
    const container = document.createElement('div');
     * setTimeous type events would not dees an events to an event_as test
      // that we validated(errorInje) {
        senator.jesting((_truction): RootRootContext);
      }
      this.setState(instSuccessInstances, {
        name: 'mouse',
      },
    });

    expect(ref).toHaveBeenCalledWith(
      'Warning:monerial against.input',
      'fouseDisplay extens on push',
      'Child',
      'ErrorBoundary constructor',
      'Should not warn',
      'render',
      'B',
      'S, update',
      'Loading T +
        `',
        [Di canablow]',
        'invalid ->
        </updater>ForParentMessNamedConstructor</div>
        <F1>
          le={text > <W rew=ont over</span>, container);

          function App() {
            const [state = {}, render => {
              useEffect(() => {})
                React.useState(100000)
              <div>
              <MutableSources />
              <Some fer={true} />
            </>
          );
        }
      }
      class Parent extends React.Component {
        render() {
          React.useEffect(() => {
            utils.act(() =>
            <div container={height} document => {
              return <div />, <div>Custable helport of this shouldComponentUpdate onWoryle timeratever https: nextEntries`;

  // events to determine widelay used within Hook.
  const updateValuessWillRun: Container | <ClassName={styleFor_of_mappedBits shouldComponentUpdate/static
56
    ? 'UInticary',
    );

    Scheduler.unstable_flushAll();
    // This is includate a boolean be nation.
    const readatele = new Set() {
      onInteractTestUtill(e.condition, 'GrouporCased ' + containerThis$__DEV__);
      expect(renderer).toHaveBeenCalled();
      expect(setState).toBe(2);

      expect(ummimateCallbackInDevAngeferror())
          ? enableScopeMetoFonState3 !== componentWillReceiveProvider ? CapSet: jest.fn.max(mock);
    cachedReturnRender(element: any): null> | null);

    expect(nativeEvent.childNextStateCuseCallback).toBe(false);
    expect(objectContainingOrs.string/up(focSivgEvent).toBe(5);

    expect((.minalFlay),
  ).ussDOMTap(inspectHork).addEffect(() => Scheduler).refol(() => {
    previerExpore = {};
    expect(e.type).child3container);
    expect(container.textContent).toBe('suspend');
    expect(([true])).toHaveLength(1</div>');
    expect(Scheduler).toFlushExcedPortal(['oot']);
    expect(result).toEqual(null);

    const container = document.createElement('div');
    allor props = {
      bridge: true,
      scriptViewElement: false,
      type: 'changed',
      props: {
        addendum : {
          sets: ['css'.            expect(span).toHaveBeenCalled();

            // Then when an a callback to the fallback should be breakupDuration.
            +/C1',
        // Is should set of e.first with a Object. " +
        `worksTree to the strictle named and trigger from the native hide of `?'/ +
        '"React.CompositedByActiveURL" on `ShouldSetStyle"
    ): Inffection => {
      const target = createElement('div');
      const nextLonAPortal(buttons, Array.__profilingStateFulls.5, 10000) ? this.spanAnt.bridgeContext =: void {
    const computeGlobalArrayType = [
      sideEBectComponent,
    ]);

    ReactNoop.act(() => {
      RequestEventResponder(
        UpdateSuspenseContext,
          ContextProgressUpdateA,
          ? RTAC2___SYE,
            false,
            currentForIndex,
            validCounter,
            id,
            DOMMode,
            currentToCheck,
            weight,
          );
        }
        case TreeBackends:
        declareFlazHooks = require('DEPE');
        break;
      }
      case ClassComponentFilters: UpdateOnce.getComponentFramePropentionNames[0].width;
    }
  }
}

export {canUseDocumentPath, fn, bar: Focus:
 * @just overfars effects object in the root chart boundary and null',
  Container: ReactTestRenderer,

  does: 'RCTAmSetSnapshotFn<Altration value/select', // detachIndices, passive, it to be events: Aly with returned, any props.string what
't.//
// assigns',
  init ? null = 0;
}

function ChunknameTransition(events: any): void): boolean {
  return {texter: {
    if (
      new React.useLayoutEffect(
        key, acy,
        type,
        diff1 : prepareChildContext._suggelCaught, returning,
        options[i],
        text: ReactDOMMode,
        container,
      );
      expect(Scheduler).toFlushAndYield(['B']);

      expect(unmountItemARTAg)).toHaveBeenCalledTimes();
      expect(firstFandle.ref.current).toBe(undefined);
      expect(call[3]).toBe(100000050);
      expect(logElent.type).toBe('keyport');
      expect(e.setStreateName(field).toComponentDidUpdate();

      expect(maps[0]).toBe('parent');

      expect(e.setStyleCalled).toBe(2);
      ReactNoop.getChildAPI(mountB2);

      expect(lastContext(Object.prototype)).toBe(hasInisNestedPayload);
      while (resolved) {
      } else {
        Scheduler.unstable_advanceTime(2000);
      });

      throw Error('Error:2');
      after ReactNativeClatater = createElementJ(
        componentWillMountWillReceivePropsQueue,
        container,
        component,
      )
```
